apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: modela-api-gateway
    app.kubernetes.io/part-of: modela
  name: modela-api-gateway-test
spec:
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      nodePort: 30000
      port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: modela-api-gateway